<h1>Todos</h1>

<h3>You have <%= pluralize(@uncompleted_todos_count, "thing") %> to do today</h3>

<ul>
  <% @todos.each do |todo| %>
    <li>
      <%= todo.title %>
      <span>
        <% if todo.completed? %>
          <%= link_to "Mark as uncompleted", toggle_completed_todo_path(todo), data: { turbo_method: :patch } %>
        <% else %>
          <%= link_to "Mark as completed", toggle_completed_todo_path(todo), data: { turbo_method: :patch } %>
        <% end %>
      </span>
      <span>
        <%= link_to "Edit", edit_todo_path(todo) %>
      </span>
      <span>
        <%= link_to "Delete", todo_path(todo), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
      </span>
    </li>
  <% end %>
</ul>

<%= link_to "New Todo", new_todo_path %>