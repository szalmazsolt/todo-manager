<h1>Add New Todo</h1>

<%= form_with(model: @todo) do |f| %>

  <% if @todo.errors.any? %>
    <div>
      <h2>
        <%= pluralize(@todo.errors.size, "error") %> prohibited this todo from being saved:
      </h2>
        <ul>
          <% @todo.errors.full_messages.each do |msg| %>
            <li>
              <%= msg %>
            </li>
          <% end %>
        </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :title %>
    <%= f.text_field :title, autofocus: true, placeholder: "What do you need to do?" %>
  </div>

  <div>
    <%= f.submit %>
  </div>

<% end %>